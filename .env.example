# Copy to .env.local and replace the placeholders. Never prefix these with VITE_;
# Vite exposes VITE_ variables to browser code.
HOUSECANARY_API_KEY=replace-with-housecanary-api-key
HOUSECANARY_API_SECRET=replace-with-housecanary-api-secret
